<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/css/main.css" />
    <title>Add a Set</title>
  </head>
  <body>
    <%- include('partials/navbar', {page: '/lego/addSet'}) %>

    <div class="container mx-auto my-5">
      <div
        class="hero rounded hero-lg bg-base-200 bg-cover bg-center"
        style="background-image: url(/images/944250.jpg)"
      >
        <div class="hero-overlay bg-opacity-60"></div>
        <div class="hero-content text-center m-6">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Add a Set!</h1>
            <p class="py-6">
              Below, you will find a form to add a new set to the collection!
            </p>
          </div>
        </div>
      </div>

      <div>
        <form class="addSetForm" action="/lego/addSet" method="POST">
          <div class="form-control">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required />
          </div>
          <div class="form-control">
            <label for="year">Year</label>
            <input type="number" id="year" name="year" required />
          </div>
          <div class="form-control">
            <label for="num_parts">Number of Parts</label>
            <input type="number" id="num_parts" name="num_parts" required />
          </div>
          <div class="form-control">
            <label for="img_url">Image URL</label>
            <input type="url" id="img_url" name="img_url" required />
          </div>
          <div class="form-control">
            <label for="theme_id">Theme</label>
            <select id="theme_id" name="theme_id" required>
              <% themes.forEach(theme => { %>
              <option value="<%= theme.id %>"><%= theme.name %></option>
              <% }) %>
            </select>
          </div>
          <div class="form-control">
            <label for="set_num">Set Number</label>
            <input type="text" id="set_num" name="set_num" required />
          </div>

          <button class="btn btn-primary" type="submit" action="/lego/addSet">
            Add Set
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
